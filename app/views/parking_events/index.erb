<% current_user.parking_events.each do |p| %>
  <div class="ui vertical segment">
    <%= erb :"parking_events/show", locals: { parked: p, header: "Created on #{p.created_at}" } %>
  </div>
<% end %>
